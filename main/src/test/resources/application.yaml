spring:
  r2dbc:
    url: r2dbc:h2:mem:///testdb;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=FALSE;CASE_INSENSITIVE_IDENTIFIERS=TRUE
    username: sa
    password:
    pool:
      enabled: true
  data:
    r2dbc:
      initialization-mode: always
  security:
    oauth2:
      client:
        registration:
          keycloak:
            provider: keycloak
            client-id: main
            client-secret: miy9KiOJVhiF8DZcDGltXFaQwMyeCJmU
            authorization-grant-type: client_credentials
            scope: openid
        provider:
          keycloak:
            issuer-uri: http://localhost:8082/realms/my-market-app